runtime: nodejs22
service: frontend

env_variables:
  NODE_ENV: "production"

instance_class: F2
automatic_scaling:
  target_cpu_utilization: 0.65
  min_instances: 1
  max_instances: 5

handlers:
  - url: /.*
    static_files: dist/index.html
    upload: dist/index.html